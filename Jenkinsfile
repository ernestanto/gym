pipeline {
    agent any

    stages {
        stage('Test Connection') {
            steps {
                echo 'Jenkins successfully connected to Gym GitHub repository!'
            }
        }
    }
}